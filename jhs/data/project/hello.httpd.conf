

    ## Settings for BOINC project hello

    Alias /hello/download /home/boincadm/project/download
    Alias /hello/stats /home/boincadm/project/html/stats
    Alias /hello/user_profile /home/boincadm/project/html/user_profile
    Alias /hello /home/boincadm/project/html/user

    Alias /hello_ops /home/boincadm/project/html/ops
    ScriptAlias /hello_cgi /home/boincadm/project/cgi-bin
    
    # In the following, the "denied" and "granted" lines are for Apache 2.4
    # For Apache 2.2, replace them with the lines
    # Order deny,allow
    # Deny from all
    #      or
    # Order allow,deny
    # Allow from all
    
    # NOTE: Turn off access to certain default directories
    <Directory "/home/boincadm/project/keys">
        Require all denied
    </Directory>
    <Directory "/home/boincadm/project/upload">
        Require all denied
    </Directory>

    # NOTE: Allow access but disable PHP script execution
    <Directory "/home/boincadm/project/download">
        RemoveType .php .phtml
        Require all granted
    </Directory>
    <Directory "/home/boincadm/project/html/stats">
        RemoveType .php .phtml
        Require all granted
    </Directory>
    <Directory "/home/boincadm/project/html/user_profile">
        RemoveType .php .phtml
        Require all granted
    </Directory>

    # NOTE: Allow access and allow PHP script execution
    <Directory "/home/boincadm/project/html">
        Options Indexes MultiViews
        AllowOverride AuthConfig
        Require all granted
    </Directory>

    # NOTE: Allow access and allow CGI execution
    <Directory "/home/boincadm/project/cgi-bin">
        Options ExecCGI
        AllowOverride AuthConfig
        Require all granted
    </Directory>


